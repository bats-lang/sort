# lib

### `fun
sort_int
  {l:agz}{n:pos}
  (arr: !$A.arr(int, l, n), len: int n)
  : void`

### `fun{a:t@ype}
sort_with
  {l:agz}{n:pos}
  (arr: !$A.arr(a, l, n), len: int n, cmp: (a, a) -<cloref1> int)
  : void`
